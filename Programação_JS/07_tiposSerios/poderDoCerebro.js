let text = "YOU SHOULD NEVER SHOUT WHEN TYPING";
let presentableText = text.toLocaleLowerCase();
if(presentableText.length > 0){
    console.log(presentableText);
}
console.log(presentableText.length);